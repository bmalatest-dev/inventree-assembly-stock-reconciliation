diff --git a/pyproject.toml b/pyproject.toml
index 0000000..0000000 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@
-version = "0.5.3"
+version = "0.6.0"

diff --git a/assembly_stock_reconciliation/plugin.py b/assembly_stock_reconciliation/plugin.py
index 0000000..0000000 100644
--- a/assembly_stock_reconciliation/plugin.py
+++ b/assembly_stock_reconciliation/plugin.py
@@
 import decimal
 from collections import defaultdict

+from django.contrib.auth.decorators import login_required
 from django.core.exceptions import ValidationError
 from django.db import transaction
+from django.http import HttpResponse, HttpResponseForbidden
+from django.urls import path

 from plugin import InvenTreePlugin
-from plugin.mixins import ActionMixin, UserInterfaceMixin, SettingsMixin
+from plugin.mixins import ActionMixin, UserInterfaceMixin, SettingsMixin, UrlsMixin
@@
 from stock.models import StockItem, StockLocation
 from .policy import normalize_footprint, spillage_per_project, evaluate_policy, aggregate_allocation_policy, fmt_decimal, select_effective_price, distribute_consumption_by_policy, compact_tracking_note, parse_transient_location_patterns, location_is_transient, recommend_return_location, commit_consumption_targets, is_basic_passive_identity
+from .spillage_report import build_spillage_report, report_to_csv
@@
-class AssemblyStockReconciliationPlugin(ActionMixin, UserInterfaceMixin, SettingsMixin, InvenTreePlugin):
+class AssemblyStockReconciliationPlugin(ActionMixin, UserInterfaceMixin, SettingsMixin, UrlsMixin, InvenTreePlugin):
@@
-    VERSION = "0.5.3"
+    VERSION = "0.6.0"
@@
     ACTION_NAME = "assembly_stock_reconciliation"
+
+    def setup_urls(self):
+        """Expose a read-only downloadable Build Order spillage report."""
+        return [
+            path(
+                "spillage-report/<int:build_id>.csv",
+                self.download_spillage_report,
+                name="post-assembly-spillage-report",
+            ),
+        ]
+
+    @login_required
+    def download_spillage_report(self, request, build_id):
+        """Download exception-only post-assembly spillage report for a BO."""
+        if not request.user.has_perm("build.view_build"):
+            return HttpResponseForbidden("You do not have permission to view Build Orders.")
+
+        report = build_spillage_report(build_id)
+        response = HttpResponse(
+            report_to_csv(report),
+            content_type="text/csv; charset=utf-8",
+        )
+        response["Content-Disposition"] = (
+            f'attachment; filename="post-assembly-spillage-'
+            f'{report["build_reference"]}.csv"'
+        )
+        return response
+
+    def get_ui_primary_actions(self, request, context, **kwargs):
+        """Add a supported Build Order download action.
+
+        InvenTree's public plugin UI API does not currently expose an extension
+        point for injecting items into an individual table's native Download
+        menu. The supported placement is therefore a primary action on the Build
+        Order page.
+        """
+        context = context or {}
+        target_model = str(context.get("target_model") or "").casefold()
+        target_id = context.get("target_id")
+
+        if target_model not in {"build", "buildorder"} or not target_id:
+            return []
+
+        return [
+            {
+                "key": "post-assembly-spillage-report",
+                "title": "Post Assembly Spillage Report",
+                "description": (
+                    "Download material consumption above the approved "
+                    "Build Order spillage allowance."
+                ),
+                "icon": "ti:file-spreadsheet:outline",
+                "options": {
+                    "url": (
+                        f"/plugin/{self.SLUG}/spillage-report/"
+                        f"{int(target_id)}.csv"
+                    ),
+                },
+            }
+        ]
